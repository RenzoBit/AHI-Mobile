<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pe.hikaru.ahi.activities"
    android:versionCode="1"
    android:versionName="1.0" >

    <!--<uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />-->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_i"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NuevoViajeActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="Seleccione un vehículo" >
        </activity>
        <activity
            android:name=".NuevoVehiculoActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="Nuevo vehículo" >
        </activity>
        <activity
            android:name=".EnvioUbicacionActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="Envio de ubicaciones" >
        </activity>
    </application>

</manifest>